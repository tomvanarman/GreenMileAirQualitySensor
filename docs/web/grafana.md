# Grafana

This project runs a self-hosted instance of Grafana. On start up the default credentials are `admin`
for both the password and username. A new password will be requested on first login.

## Setup

TODO
