# SIM7080G – Hardware Guide

This page documents how the SIMCom SIM7080G cellular module is powered, connected, and validated in
this project. It complements the datasheet and hardware design resources from the vendor:

- Product page: https://www.simcom.com/product/SIM7080G.html
- Reference manuals and AT command set: see downloads on the product page.

The SIM7080G is a low‑power LTE Cat‑M1/NB‑IoT + GNSS module. In this project it is mounted on a
board with an AXP2101 PMU and is controlled by an ESP32.

## Table of contents

- [1. Module capabilities (project-relevant)](#1-module-capabilities-project-relevant)
- [2. Hardware integration](#2-hardware-integration)
    - [2.1. Power tree (AXP2101 PMU)](#21-power-tree-axp2101-pmu)
    - [2.2. ESP32 connections (typical)](#22-esp32-connections-typical)
    - [2.3. Antennas](#23-antennas)
    - [2.4. SIM card](#24-sim-card)
- [3. Power‑on and bring‑up sequence](#3-poweron-and-bringup-sequence)
- [4. Software dependencies (project)](#4-software-dependencies-project)
- [5. Firmware integration notes](#5-firmware-integration-notes)
- [6. Sensor bus assumptions](#6-sensor-bus-assumptions)
- [7. PlatformIO notes](#7-platformio-notes)
- [8. Validation and diagnostics](#8-validation-and-diagnostics)
- [9. Test documentation SIM not working](#9-test-documentation-sim-not-working)
    - [9.1. Conclusion: what fixed the problem](#91-conclusion-what-fixed-the-problem)
- [10. Project criteria mapping](#10-project-criteria-mapping)
- [11. Quick test procedure](#11-quick-test-procedure)
- [12. Troubleshooting](#12-troubleshooting)
- [13. Safety and RF notes](#13-safety-and-rf-notes)
- [14. References](#14-references)

## 1. Module capabilities (project-relevant)

- LTE Cat‑M1 and NB‑IoT with global bands (board/SIM dependent).
- Packet data over IP (APN required).
- TCP/UDP/HTTP(S) stacks on-module.
- GNSS (GPS/GLONASS/BeiDou/Galileo/QZSS) with active antenna power rail available on the board.
- Diagnostic AT commands for registration, signal, bearer activation, and ping.

## 2. Hardware integration

### 2.1. Power tree (AXP2101 PMU)

The PMU powers the modem and GNSS rails. The firmware configures:

- DC3 = 3.0 V → main SIM7080G supply (per project code).
- BLDO1 = 3.3 V → UART level shifter between ESP32-S3 and SIM7080G.
- BLDO2 = 3.3 V → GNSS antenna bias (enable for GNSS use).
- TS pin measurement disabled to allow battery charging.

These settings are applied in code:

- PWRKEY and DTR held low before enabling modem rails.
- BLDO1 enabled at 3.3 V.
- AXP2101 DC3 low-voltage PMIC turn-off disabled before enabling DC3.
- DC3 enabled at 3.0 V.
- BLDO2 left off for cellular-only tests; enable it only when GNSS is needed.
- T_SENSE measurement disabled.

Notes:

- Ensure the 3.0 V rail can supply modem peaks (hundreds of mA during Tx bursts).
- Keep supply decoupling capacitors close to the module as per SIMCom hardware design guide.
- Use a good ground plane; minimize supply and RF return path impedance.

### 2.2. ESP32 connections (typical)

- UART: ESP32 Serial1 (TXD/RXD) → SIM7080G UART0 (115200 8N1).
- PWRKEY: GPIO → controls modem power-on sequence (low pulse >1 s).
- DTR: GPIO → sleep control (optional; keep defined).
- RI: GPIO input → ring indicator (optional).
- I2C: SDA/SCL → AXP2101 PMU control.
- GNSS antenna power: provided by BLDO2 (3.3 V) on the board (enable only when needed).

Exact pin numbers are board-specific. See your board header/pinmap for:

- BOARD_MODEM_TXD_PIN / BOARD_MODEM_RXD_PIN
- BOARD_MODEM_PWR_PIN / BOARD_MODEM_DTR_PIN / BOARD_MODEM_RI_PIN
- I2C_SDA / I2C_SCL

### 2.3. Antennas

- Main LTE antenna: required for cellular connectivity.
- GNSS antenna: required for GNSS; enable BLDO2 to power active antennas.
- Keep antenna connectors tight; route coax away from noisy digital lines.

### 2.4. SIM card

- Use a Cat‑M1/NB‑IoT capable SIM (e.g., 1NCE).
- APN must be set (project uses APN: iot.1nce.net).
- Verify SIM presence with AT+CPIN? and TinyGSM getSimStatus().

## 3. Power‑on and bring‑up sequence

1. PMU init:
    - Initialize AXP2101 over I2C.
    - Hold modem PWRKEY and DTR low before enabling modem rails.
    - Enable BLDO1 (3.3 V) for the modem UART level shifter.
    - Disable AXP2101 DC3 low-voltage PMIC turn-off.
    - Disable TS pin measurement.
    - Enable DC3 (3.0 V) for modem.
    - Enable BLDO2 (3.3 V) only if GNSS is needed.

2. Modem start:
    - Configure UART (115200 8N1).
    - Pulse PWRKEY: low >1 s to boot the module.
    - Wait for AT responsiveness (test with AT).

3. Network setup:
    - CFUN=0 (RF off), set network and preferred mode (Cat‑M1 by default).
    - Set APN with CGDCONT.
    - CFUN=1 (RF on), wait for registration (home or roaming).
    - Configure PDP profile with CNCFG, then activate with CNACT=0,1.
    - Optionally enable local time sync CLTS and read CCLK.

These steps are automated by SIM7080::initialize(), ::start(), ::setupNetwork().

## 4. Software dependencies (project)

- TinyGSM (SIM7080 profile): UART/AT management and helpers.
- XPowersLib (AXP2101): PMU control for rails and charging.
- Arduino framework on ESP32.

## 5. Firmware integration notes

The production firmware uses WiFi first and the SIM7080G as fallback. The full runtime behavior is
documented in [ESP32 Firmware Behavior](../software/esp32-firmware-behavior.md).

SIM fallback assumes:

- The SIM7080G is powered through the AXP2101-controlled rails described on this page.
- A Cat-M1/NB-IoT capable SIM is installed.
- The LTE antenna is connected before testing.
- The APN is configured in firmware as `iot.1nce.net`.
- Device credentials are stored, because the device ID is used in the MQTT topic names.
- The MQTT broker is reachable at `greenmile.tapp.city:1883`.

When WiFi is unavailable, the firmware initializes the modem, waits for network registration,
synchronizes time from the modem when possible, and publishes payloads through
`SIM7080::mqttPublish()`.

MQTT topic routing is shared between WiFi and SIM:

| Payload | MQTT topic                           |
| ------- | ------------------------------------ |
| SPS30   | `greenmile/<device_id>/sps30/data`   |
| SHT41   | `greenmile/<device_id>/sht41/data`   |
| Battery | `greenmile/<device_id>/battery/data` |

## 6. Sensor bus assumptions

The SIM7080G board also hosts the sensor firmware. The current production firmware expects the
sensor I2C bus on:

- SDA: GPIO `8`
- SCL: GPIO `9`

Expected sensor addresses:

- SHT41: `0x44`
- SPS30: `0x69`

At startup, the firmware performs I2C bus recovery, probes both sensor addresses, and waits for the
sensors before taking measurements.

## 7. PlatformIO notes

The main firmware environment is `esp32s3usbotg` in `embedded/esp32/platformio.ini`.

The project intentionally does not commit a fixed `upload_port`, because Windows COM ports differ
per laptop. Build normally with:

```powershell
pio run -e esp32s3usbotg
```

Upload with a local port when needed:

```powershell
pio run -e esp32s3usbotg -t upload --upload-port COM8
```

Replace `COM8` with the port shown by your machine.

## 8. Validation and diagnostics

Use these checks to validate hardware wiring and the RF path:

- Modem alive:
    - AT → OK
- SIM ready:
    - AT+CPIN? → READY
- Registration:
    - AT+CEREG? (LTE attach), AT+CGREG? or AT+CREG? → registered (home/roaming)
- Operator and RAT:
    - AT+COPS? → operator, RAT (Cat‑M1/NB)
- Signal quality:
    - AT+CSQ → RSSI BER (e.g., 10–20 typical indoors on Cat‑M1)
- PDP/bearer:
    - AT+CNACT? → state 1 when active, show IP address
- Ping:
    - AT+SNPING4="8.8.8.8",1,3000,32 → verify IP reachability

The project emits structured logs during these steps. Use the serial monitor at 115200 baud.

## 9. Test documentation SIM not working

Detailed bring-up tests are documented separately:

- Part 1: [Serial heartbeat test](sim7080g-tests/01-serialtest.md)
- Part 2: [PMU CHG LED test](sim7080g-tests/02-blinktest.md)
- Part 3: [USB-C and VBUS watch](sim7080g-tests/03-vbuswatch.md)
- Part 4: [BLDO2 GNSS antenna rail test](sim7080g-tests/04-bldo2-gnss-rail.md)
- Part 5: [DC3 modem rail test](sim7080g-tests/05-dc3-modem-rail.md)
- Part 6: [Full Cat-M bring-up test](sim7080g-tests/06-full-cat-m-bringup.md)
- Part 7: [DC3 UVP stability test](sim7080g-tests/07-dc3-uvp-stability.md)
- Part 8: [Modem UART diagnostics](sim7080g-tests/08-modem-uart-diagnostics.md)
- Part 9: [Successful Cat-M validation](sim7080g-tests/09-successful-catm-validation.md)
- Part 10: [SIM7080 full system integration](sim7080g-tests/10-sim7080-full-system-integration.md)

### 9.1. Conclusion: what fixed the problem

The SIM7080G bring-up became stable after two issues were fixed:

- The AXP2101 PMU setup was changed so DC3 low-voltage PMIC turn-off is disabled before enabling the
  SIM7080G modem rail. With BLDO1 enabled for the UART level shifter, DC3 set to `3000 mV`, and
  PWRKEY/DTR held low before rail enable, the modem rail stayed alive instead of dropping out.
- A metal bolt near the SIM7080G/NB-IoT area was moved away from the board. Before this, the modem
  UART diagnostics suggested a likely partial short or mechanical contact issue because the modem
  rail was stable but no AT bytes were received.

After these fixes, the final Cat-M validation confirmed AT response, SIM detection, LTE
registration, bearer activation, IP assignment, ping, and HTTP response handling. The later full
system integration test then confirmed that the same modem setup worked together with the sensor
firmware and deep sleep flow.

## 10. Project criteria mapping

- “The CMB can connect to the internet using an LTE network.”
    - Achieved by SIM7080::setupNetwork() (APN, registration, bearer activation). Requires antenna
      and coverage.

- “The SIM7080G module can ping 8.8.8.8.”
    - Supported via AT+SNPING4. Add a small helper in code or issue the command from a debug console
      after CNACT=0,1.

- “The connection automatically re‑establishes after loss of signal or restart.”
    - Use SIM7080::ensureConnected() periodically. It re‑runs setup when GPRS/bearer is down.

- “The connection status is visible or logged for diagnostics.”
    - Logs include SIM status, registration, bearer state, HTTPS transactions. Add periodic
      CSQ/CNACT logging if needed.

- “Documentation is created on the relevant page.”
    - This page provides the hardware overview, wiring, bring‑up, and validation steps.

## 11. Quick test procedure

1. Power and boot:
    - Observe “Power chip initialized” and “Modem started” on serial logs.

2. SIM and registration:
    - Check “Registered, home network” or “Registered, roaming”.
    - If it times out, verify antenna/SIM and coverage.

3. Bearer activation:
    - Confirm “GPRS status: connected” and CNACT? returns an IP.

4. Ping test (optional):
    - Send AT+SNPING4="8.8.8.8",1,3000,32
    - Expect a response line “+SNPING4:…” with RTT and success.

5. HTTPS test:
    - The sample app posts JSON to the configured server.
    - Check SHCONN/SHREQ logs and server-side receipt.

    A. Data queueing
    - Shut down the docker container or disconnect the antenna.
    - Observe “Failed to post data” logs and data being queued.

    B. Recovery
    - Restore connectivity.
    - Observe queued data being sent successfully.

## 12. Troubleshooting

- No AT responses:
    - Check UART wiring, baud rate, PWRKEY timing, PMU rails (DC3 enabled at 3.0 V).
- Not registered:
    - Verify SIM/APN, antenna, coverage for Cat‑M1/NB‑IoT. Try alternative RAT (NB) or band scan.
- Bearer won’t activate:
    - Recheck APN (CGDCONT + CNCFG), ensure CFUN=1 and module registered. Power cycle if needed.
- Weak signal:
    - Check CSQ; improve antenna placement, use proper ground clearance, move outdoors.
- HTTPS failures:
    - Verify time sync (CLTS/CCLK), SNI configuration, and server TLS compatibility.

## 13. Safety and RF notes

- Keep antennas away from users or sensitive analog circuits during Tx.
- Follow local regulations for LTE/NB‑IoT operation and bands.
- Use ESD precautions when handling RF connectors.

## 14. References

- SIM7080G Product page and downloads: https://www.simcom.com/product/SIM7080G.html
- SIMCom SIM7080 Series AT Command Manual (from product page)
- SIMCom Hardware Design Guide for SIM7080 series
- TinyGSM library docs (SIM7080 profile)
